#include<stdio.h>
int main() //main running 
    {   
        for(int x = 0; x < 10; x++){ // create a var named x holding ints at value 0, check to make sure
                                     // it's less than 10, then after running add a 1 to value of x
            printf("%d\n",x);        // print x
        }
    }
